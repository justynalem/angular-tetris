import { FilrerByTypePipe } from './filrer-by-type.pipe';

describe('FilrerByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilrerByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
